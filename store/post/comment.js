export const state = () => ({});

export const getters = {};

export const mutations = {};

export const actions = {
  async addCommentAsync(context, comment) {
    console.log("comment", comment);
  }
};
